// top level class : (package-private)
package com.eomcs.oop.ex11.a.sub;

//top level class 의 접근 제한자
// - public : 다른 패키지에서도 접근 가능
// - (package-private; modifier 가 없다) : 다른 패키지 접근 불가!
class B { //해당 패키지 안에서만 쓰이는 패키지 프라이빗 클래스. 다른패키지도 X

}
